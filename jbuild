(jbuild_version 1)

(executable
 ((name vmc)
  (libraries (batteries angstrom))
  (preprocess (pps (ppx_deriving))) ; opam pin add ppx_deriving https://github.com/diml/ppx_deriving.git\#restore-compat+omp-driver-integration
 ))

; (executable
;  ((name tests/test)
;   (libraries (batteries angstrom))
;   (preprocess (pps (ppx_deriving)))
;  ))
;
; (alias
;  ((name    runtest)
;   (deps    (test.exe))
;   (action  (run ${<}))))
